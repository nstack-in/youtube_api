/// The `channelType` parameter lets you restrict a search to a particular
/// type of channel.
///
/// `string`
///
/// Acceptable values are:
/// *   `any` – Return all channels.
/// *   `show` – Only retrieve shows.
enum ChannelType { any, show }
